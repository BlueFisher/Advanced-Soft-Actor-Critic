default:
  base_config:
    env_type: TEST
    env_name: TEST

    max_iter: 5
    max_step_each_iter: 100
    n_agents: 2

  ma_config:
    test0:
      sac_config:
        nn: nn
        n_step: 5
    test1:
      sac_config:
        nn: nn
        n_step: 10

  replay_config:
    capacity: 65536

rnn:
  base_config:
    nn: nn_rnn

  sac_config:
    burn_in_step: 2
    seq_encoder: RNN

    batch_size: 10
